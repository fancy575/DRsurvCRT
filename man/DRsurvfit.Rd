% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/DRsurvfit.R
\name{DRsurvfit}
\alias{DRsurvfit}
\title{Doubly-robust survival estimation for CRTs (SPCE or RMST)}
\usage{
DRsurvfit(
  data,
  formula,
  cens_formula = NULL,
  intv,
  method = c("marginal", "frailty"),
  estimand = c("SPCE", "RMST"),
  tau = NULL,
  trt_prob = NULL,
  variance = c("none", "jackknife"),
  fit_controls = NULL,
  strata = NULL,
  spce_summary_times = NULL,
  verbose = FALSE
)
}
\arguments{
\item{data}{A data.frame.}

\item{formula}{Outcome model: e.g., \code{Surv(time, event) ~ W1 + W2 + Z1 + Z2 + cluster(M)}.
\itemize{
\item For \code{method = "frailty"}, a \code{cluster(<id>)} term is \strong{required} (or supply \code{strata=}).
\item For \code{method = "marginal"}, the cluster term is optional; if you also pass \code{strata=},
\code{cluster(strata)} is appended to both outcome and censoring formulas.
}}

\item{cens_formula}{Optional censoring model. If \code{NULL}, reuse the RHS (without \code{cluster()})
and set LHS to \code{Surv(time, event == 0)}. If a cluster id is known, it is appended
as \code{+ cluster(id)}.}

\item{intv}{Character: cluster-level treatment column (0/1), constant within cluster.}

\item{method}{\code{"marginal"} or \code{"frailty"}.}

\item{estimand}{\code{"SPCE"} or \code{"RMST"}.}

\item{tau}{Numeric vector of RMST landmark times (required for RMST).}

\item{trt_prob}{Optional length-2 numeric \code{(p0, p1)}. If \code{NULL}, computed from first row per cluster.}

\item{variance}{\code{"none"} or \code{"jackknife"}.}

\item{fit_controls}{Optional \code{frailtyEM::emfrail_control()} list (frailty method).}

\item{strata}{Optional cluster id column name to use if the outcome formula does not include \code{cluster()}.}

\item{spce_summary_times}{Optional numeric vector of times to show in \code{summary()} for SPCE.}

\item{verbose}{Logical.}
}
\value{
An object of class \code{DRsurvfit}.
Key fields: \code{event_time}, \code{S_full_cluster}, \code{S_full_ind},
\code{S_cluster}/\code{S_individual} (SPCE), \code{RMST_cluster}/\code{RMST_ind} (RMST),
\code{trt_prob}, \code{var_cluster}/\code{var_ind} (if jackknife),
\code{formula_outcome}, \code{cens_formula}, \code{cluster_col}, \code{n_clusters}, \code{df_jackknife},
\code{n_obs}, \code{n_events}, \code{cluster_trt_counts}.
}
\description{
Fits doubly-robust estimators for cluster-randomized trials with right-censored
outcomes. SPCE is returned on the \strong{full event-time grid}. RMST is returned at
user-specified \code{tau} (vector). Jackknife variance uses a \strong{shared} event-time grid
across leave-one-cluster-out fits and \strong{reuses} RMST-at-τ computed in the core.

The returned object includes metadata: final fitted outcome/censoring formulas,
the cluster id column, number of clusters, df for jackknife t-intervals (= K-1),
sample sizes, and cluster-level treatment split.
}
\examples{
\dontrun{
data(dat)
fit_spce <- DRsurvfit(
  dat,
  survival::Surv(time, event) ~ W1 + W2 + Z1 + Z2 + cluster(M),
  intv = "A",
  method = "marginal",
  estimand = "SPCE",
  variance = "jackknife",
  spce_summary_times = c(0.5, 1, 1.5, 2)
)
summary(fit_spce, times = c(0.5, 1, 1.5, 2))

fit_rmst <- DRsurvfit(
  dat,
  survival::Surv(time, event) ~ W1 + W2 + Z1 + Z2 + cluster(M),
  intv = "A",
  method = "frailty",
  estimand = "RMST",
  tau = c(0.5, 1, 2),
  variance = "jackknife"
)
summary(fit_rmst)
}
}
